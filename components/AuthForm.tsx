'use client';
import { useState } from 'react';
export default function AuthForm({ mode='login', onDone }: { mode?: 'login'|'register'; onDone?: ()=>void }) {
 const [register,setRegister]=useState(mode==='register'); const [form,setForm]=useState({name:'',email:'',phone:'',password:''}); const [error,setError]=useState(''); const [busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch(`/api/auth/${register?'register':'login'}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)setError(d.error||'Unable to sign in');else onDone?.();setBusy(false)}
 return <form className="card" onSubmit={submit}><h2>{register?'Create account':'Sign in'}</h2>{register&&<div className="field"><label>Name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div>}<div className="field"><label>Email</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div>{register&&<div className="field"><label>Phone</label><input placeholder="0712345678" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div>}<div className="field"><label>Password</label><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div>{error&&<p style={{color:'#9b2226'}}>{error}</p>}<button className="btn primary" disabled={busy}>{busy?'Please wait…':register?'Create account':'Sign in'}</button><p className="muted"><button type="button" onClick={()=>setRegister(!register)} style={{border:0,background:'none',padding:0,color:'var(--green)'}}>{register?'Already have an account? Sign in':'New here? Create an account'}</button></p></form>
}
