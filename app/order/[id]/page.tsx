'use client';
import { useEffect,useState } from 'react';
import Link from 'next/link';
export default function OrderPage({params}:{params:Promise<{id:string}>}){const [o,setO]=useState<any>(null);useEffect(()=>{params.then(({id})=>fetch(`/api/orders/${id}`).then(r=>r.json()).then(d=>setO(d.order)))},[params]);useEffect(()=>{if(!o)return;const i=setInterval(()=>fetch(`/api/orders/${o.id}`).then(r=>r.json()).then(d=>setO(d.order)),5000);return()=>clearInterval(i)},[o]);if(!o)return <main className="shell"><div className="container">Loading order…</div></main>;return <main className="shell"><div className="container"><Link href="/">← Back</Link><div className="card" style={{maxWidth:650,margin:'24px auto'}}><div className="row"><h1>{o.ticketCode}</h1><span className="pill">{o.status}</span></div><p>Type: {o.type}</p><h3>Timeline</h3>{o.statusLogs?.map((l:any)=><div className="item" key={l.id}><div><b>{l.to}</b><div className="muted">{l.note||''}</div></div><small>{new Date(l.createdAt).toLocaleString()}</small></div>)}{o.status==='READY'&&<div className="ticket"><b>Ready for pickup</b><p>Show ticket <strong>{o.ticketCode}</strong> at the counter.</p></div>}</div></div></main>}
